## 📊 博客系统接口全景分析

### 🎯 系统架构概览

- **前端客户端 (blog-web)**: 面向普通用户的博客浏览和互动界面
- **管理端 (admin-web)**: 面向管理员的后台管理界面
- **后端服务 (Spring Boot)**: 提供统一的 RESTful API 服务

------

## 1️⃣ 认证模块 (Auth)

### 后端定义 (`AuthController.java`)

| 接口路径         | 方法 | 功能             | 返回类型     |
| ---------------- | ---- | ---------------- | ------------ |
| `/auth/login`    | POST | 用户登录         | `LoginVO`    |
| `/auth/register` | POST | 用户注册         | `LoginVO`    |
| `/auth/logout`   | POST | 用户登出         | `String`     |
| `/auth/info`     | GET  | 获取当前用户信息 | `UserInfoVO` |
| `/auth/refresh`  | POST | 刷新Token        | `LoginVO`    |
| `/auth/check`    | GET  | 检查登录状态     | `Boolean`    |

### 前端调用

- **客户端 (blog-web)**: 调用 login, register, getUserInfo, logout
- **管理端 (admin-web)**: 调用 login, getUserInfo, logout
- **差异**: 客户端支持注册功能，管理端不支持

------

## 2️⃣ 文章模块 (Article)

### 后端定义 (`ArticleController.java`)

| 接口路径                | 方法   | 功能             | 参数                                      | 返回类型              |
| ----------------------- | ------ | ---------------- | ----------------------------------------- | --------------------- |
| `/article/publish`      | POST   | 发布文章         | `ArticleDTO`                              | `Long` (文章ID)       |
| `/article/update`       | PUT    | 更新文章         | `ArticleDTO`                              | `String`              |
| `/article/{id}`         | DELETE | 删除文章         | `id`                                      | `String`              |
| `/article/{id}`         | GET    | 获取文章详情     | `id`                                      | `ArticleVO`           |
| `/article/list`         | GET    | 分页查询文章列表 | current, size, categoryId, tagId, keyword | `Page<ArticleListVO>` |
| `/article/hot`          | GET    | 获取热门文章     | limit (默认5)                             | `List<ArticleListVO>` |
| `/article/recommend`    | GET    | 获取推荐文章     | limit (默认5)                             | `List<ArticleListVO>` |
| `/article/my`           | GET    | 获取我的文章     | current, size                             | `Page<ArticleListVO>` |
| `/article/like/{id}`    | POST   | 点赞文章         | `id`                                      | `String`              |
| `/article/collect/{id}` | POST   | 收藏文章         | `id`                                      | `String`              |

### 前端调用

- **客户端 (blog-web)**: 调用全部10个接口
- **管理端 (admin-web)**: 仅调用基础CRUD (publish, update, delete, detail, list)
- **差异**: 客户端额外支持热门文章、推荐文章、我的文章、点赞、收藏功能

------

## 3️⃣ 分类模块 (Category)

### 后端定义 (`CategoryController.java`)

| 接口路径           | 方法   | 功能         | 返回类型           |
| ------------------ | ------ | ------------ | ------------------ |
| `/category/create` | POST   | 创建分类     | `Long` (分类ID)    |
| `/category/update` | PUT    | 更新分类     | `String`           |
| `/category/{id}`   | DELETE | 删除分类     | `String`           |
| `/category/list`   | GET    | 获取所有分类 | `List<CategoryVO>` |

### 前端调用

- **客户端 (blog-web)**: 调用全部4个接口
- **管理端 (admin-web)**: 调用全部4个接口 + 标签管理接口
- **一致性**: 两端调用完全一致

------

## 4️⃣ 标签模块 (Tag)

### 后端定义 (`TagController.java`)

| 接口路径      | 方法   | 功能         | 返回类型        |
| ------------- | ------ | ------------ | --------------- |
| `/tag/create` | POST   | 创建标签     | `Long` (标签ID) |
| `/tag/update` | PUT    | 更新标签     | `String`        |
| `/tag/{id}`   | DELETE | 删除标签     | `String`        |
| `/tag/list`   | GET    | 获取所有标签 | `List<TagVO>`   |

### 前端调用

- **客户端 (blog-web)**: 调用全部4个接口
- **管理端 (admin-web)**: 在 category.js 中调用全部4个接口

------

## 5️⃣ 评论模块 (Comment)

### 后端定义 (`CommentController.java`)

| 接口路径                    | 方法   | 功能             | 返回类型          |
| --------------------------- | ------ | ---------------- | ----------------- |
| `/comment/publish`          | POST   | 发布评论         | `Long` (评论ID)   |
| `/comment/{id}`             | DELETE | 删除评论         | `String`          |
| `/comment/like/{id}`        | POST   | 点赞评论         | `String`          |
| `/comment/list/{articleId}` | GET    | 获取文章评论列表 | `Page<CommentVO>` |

### 前端调用

- **客户端 (blog-web)**: 调用全部4个接口
- **管理端 (admin-web)**: 未定义评论接口
- **差异**: 评论功能仅在客户端使用

------

## 6️⃣ 轮播图模块 (Banner)

### 后端定义 (`BannerController.java`)

| 接口路径       | 方法 | 功能           | 返回类型         |
| -------------- | ---- | -------------- | ---------------- |
| `/banner/list` | GET  | 获取轮播图列表 | `List<BannerVO>` |

### 前端调用

- **客户端 (blog-web)**: 调用 list 接口
- **管理端 (admin-web)**: 未定义轮播图接口
- **差异**: 轮播图仅在客户端展示

------

## 7️⃣ 文件上传模块 (File)

### 后端定义 (`FileController.java`)

| 接口路径               | 方法   | 功能         | 限制           | 返回类型           |
| ---------------------- | ------ | ------------ | -------------- | ------------------ |
| `/file/upload/image`   | POST   | 上传图片     | 5MB, 图片格式  | `String` (文件URL) |
| `/file/upload/article` | POST   | 上传文章图片 | 10MB, 图片格式 | `String` (文件URL) |
| `/file/delete`         | DELETE | 删除文件     | -              | `String`           |

### 前端调用

- **客户端 (blog-web)**: 调用全部3个接口
- **管理端 (admin-web)**: 仅调用 `/file/upload/image`
- **差异**: 客户端支持文章图片上传和文件删除

------

## 8️⃣ 用户管理模块 (User)

### 后端定义 (`UserController.java`)

| 接口路径                    | 方法   | 功能           | 返回类型        |
| --------------------------- | ------ | -------------- | --------------- |
| `/user/page`                | GET    | 分页查询用户   | `Page<SysUser>` |
| `/user/list`                | GET    | 查询所有用户   | `List<SysUser>` |
| `/user/{id}`                | GET    | 根据ID查询用户 | `SysUser`       |
| `/user/username/{username}` | GET    | 根据用户名查询 | `SysUser`       |
| `/user`                     | POST   | 新增用户       | `String`        |
| `/user`                     | PUT    | 更新用户       | `String`        |
| `/user/{id}`                | DELETE | 删除用户       | `String`        |
| `/user/batch`               | DELETE | 批量删除用户   | `String`        |

### 前端调用

- **客户端 (blog-web)**: 未定义用户管理接口
- **管理端 (admin-web)**: 未定义用户管理接口
- **注意**: 后端已实现但前端未调用
